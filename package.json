{
  "name": "valorant-shop-checker-dev",
  "private": true,
  "scripts": {
    "dev:backend": "cd backend && .venv\\Scripts\\python -m uvicorn app.main:app --reload",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:callback": "python riot_callback.py",
    "dev": "concurrently -k \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:callback\""
  },
  "devDependencies": {
    "concurrently": "^10.0.4"
  }
}